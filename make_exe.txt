
仮想環境venvへの切り替え
venv\Scripts\Activate.ps1 

インストール済みライブラリとバージョン一覧を表示
python -m pip list     

venvにインストーラ（EXE用）を導入
python -m pip install pyinstaller pyinstaller-hooks-contrib

EXE生成前テスト
pyinstaller --noconsole --onedir --add-data "video_frame_cropper_config.json;." --add-data "README.md;." --hidden-import=cv2 --hidden-import=PIL.ImageTk video_frame_cropper.py
pyinstaller --noconsole --onedir --add-data "README.md;." --hidden-import=cv2 --hidden-import=PIL.ImageTk video_frame_cropper.py

python -m PyInstaller --noconsole --onedir --hidden-import=cv2 --hidden-import=PIL.ImageTk video_frame_cropper.py

EXEを生成
pyinstaller --noconsole --onefile --clean --noconfirm --collect-all cv2 --collect-all PIL --hidden-import=PIL._tkinter_finder --add-data "video_frame_cropper_config.json;." --add-data "README.md;." video_frame_cropper.py

python -m PyInstaller --noconsole --onefile --clean --noconfirm --collect-all cv2 --collect-all PIL --hidden-import=PIL._tkinter_finder --add-data "README_shortcuts.md;." --add-data "README.md;." video_frame_cropper.py

v0.2修正版
python -m PyInstaller --noconsole --onefile --clean --noconfirm `
    --collect-all cv2 --collect-all PIL `
    --hidden-import=PIL._tkinter_finder `
    --add-data "README_shortcuts.md;." `
    --add-data "README.md;." `
    video_frame_cropper.py


いずれやること
## 仕様変更
- クロップの赤枠を複数設置できるように仕様変更します。

## 詳細
- 右にリストボックスを追加して、設置したクロップの一覧を並べてください。
- リストボックスには、以下の設定値を表示してください。
  1. 任意の名前。リストアイテムをダブルクリックすると入力ボックスを表示して打てるようにする
  2. 

## 指定フォルダの動画一覧を表示する
- 選択した動画を表示する
- 選択した複数動画に対して同じクロップ処理を適用する

マウスポインタの軌跡を動画の時刻とフレームとXとY座標でテキストファイルに記録して、録画完了と同時に動画と同じ名前で同じ場所に tsv形式で保存するようにしてください。
録画の画質が悪すぎる
